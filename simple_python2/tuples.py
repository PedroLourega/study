#Tuplas são semelhantes às listas, mas são imutáveis:
#Tuples are similar to lists, but they are immutable:

coordenadas = (10, 20) 
#cria a tupla coordenada, que armazena dois valores.
#creates the coordinate tuple, which stores two values.

print(coordenadas[0])
#printa a tupla, especificando o campo [0].
#print the tuple, specifying the field [0].